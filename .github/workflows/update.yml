- name: Build Adblock360 List
run: |
mkdir -p lists
# Domenləri oxu, boşluqları sil, təkrar olanları çıxar, 0.0.0.0 prefiksi əlavə et
grep -v '^$' data/adblock360.txt | sort -u | sed 's/^/0.0.0.0 /' > lists/adblock360.hosts
